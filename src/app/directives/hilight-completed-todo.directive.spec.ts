import { HilightCompletedTodoDirective } from './hilight-completed-todo.directive';

describe('HilightCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
